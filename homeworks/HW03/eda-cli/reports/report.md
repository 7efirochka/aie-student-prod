# # EDA-отчёт 

Исходный файл: `example_2.csv`

Строк: **11**, столбцов: **5**

## Качество данных (эвристики)

- Оценка качества: **0.00**
- Макс. доля пропусков по колонке: **81.82%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **True**
- Столбцы с большим количеством уникальных значений (> 60%): **True** 
	**categories - 0.818%**
	**date - 1.0%**
- Униальность идентификаторов в столбце user_id: **True**
- Столбцы с исключительно не уникальными значениями: **True**

## Проблемные колонки, с большим числом пропусков (>50.0%)
	**date**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
